import { extendTheme } from "@mui/joy/styles";

export default extendTheme({
  radius: {
    xs: "8px",
    sm: "12px", // Components use default sm radius?
    md: "16px",
    lg: "24px",
    xl: "32px",
  },
});
