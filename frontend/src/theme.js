import { extendTheme } from "@mui/joy/styles";

export default extendTheme({
  radius: {
    xs: "4px", // default 2px
    sm: "8px", // default 6px
    md: "12px", // default 8px
    lg: "16px", // default 12px
    xl: "20px", // default 16px
  },
});
